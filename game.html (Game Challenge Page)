<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Birthday Game Challenge</title>
    <link rel="stylesheet" href="styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <div class="container game-container">
        <header>
            <h1 class="title">Birthday Game Challenge</h1>
            <p id="gameIntro" class="subtitle">Win this game to see your special birthday message!</p>
        </header>

        <main>
            <div id="gameSection" class="game-section">
                <div class="game-info">
                    <p>Memory Card Game: Match all the pairs to unlock your birthday message!</p>
                    <div class="game-stats">
                        <span>Moves: <span id="moves">0</span></span>
                        <span>Pairs Found: <span id="pairs">0</span>/<span id="totalPairs">8</span></span>
                    </div>
                </div>

                <div id="gameBoard" class="memory-game">
                    <!-- Cards will be generated by JavaScript -->
                </div>

                <div id="gameControls" class="game-controls">
                    <button id="startGameBtn" class="btn btn-primary">Start Game</button>
                    <button id="restartGameBtn" class="btn btn-secondary hidden">Restart Game</button>
                </div>
            </div>

            <div id="gameResult" class="game-result hidden">
                <div id="winResult" class="win-result hidden">
                    <h2>Congratulations! 🎉</h2>
                    <p>You've won the game! Now you can see your special birthday message.</p>
                    <button id="viewMessageBtn" class="btn btn-primary">View Birthday Message</button>
                </div>

                <div id="loseResult" class="lose-result hidden">
                    <h2>So Close! 😢</h2>
                    <p>Don't worry, you can try again!</p>
                    <button id="tryAgainBtn" class="btn btn-primary">Try Again</button>
                </div>
            </div>
        </main>

        <footer>
            <p>&copy; 2025 Birthday Wish Creator. All rights reserved.</p>
        </footer>
    </div>

    <script src="game.js"></script>
</body>
</html>
